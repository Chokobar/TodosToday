import * as React from "react";
import Button from '@material-ui/core/Button';

const TestMaterialUi = () => {
    return <div>
        <Button variant="contained" color="primary">
            BUTTON!    
        </Button>
    </div>
}

export { TestMaterialUi};